# NodePort Service for BookMyShow App
apiVersion: v1
kind: Service
metadata:
  name: bookmyshow-service
spec:
  type: NodePort
  selector:
    app: bms
  ports:
    - protocol: TCP 
      port: 3000
      targetPort: 3000
      nodePort: 30001
